{
  # networking.wireless.enable = true;  # Enables wireless support via wpa_supplicant.
  networking.networkmanager.enable = true;  # not sure what wireless.enable does exactly, but seems orthogonal or even conflicting with networkmanager, and I want networkmanager
}
